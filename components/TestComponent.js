import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default class TestComponent extends Component {
    render() {
        return (
            <View>
                <Text> This is just for testing adding a new component </Text>
            </View>
        )
    }
}
